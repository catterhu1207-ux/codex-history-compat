# Contributing

Keep fixtures synthetic and bind every patch revision to an exact upstream commit. Run `git apply --check` and the focused `codex-core` tests before submitting a change. Do not add captured requests, conversation identifiers, credentials, or binaries.
